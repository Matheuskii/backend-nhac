spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=false;MODE=MySQL
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

# Desabilitar Flyway porque scripts MySQL específicos (como Triggers e Constraints) falham no H2
spring.flyway.enabled=false

# Chave JWT de teste
api.security.token.secret=minha_chave_secreta_local_muito_segura_12345_para_testes_apenas

# Twilio Mock
nhac.sms.mock-mode=true

# Stripe Configuration
stripe.api.key=sk_test_mock
stripe.webhook.secret=whsec_mock

# Asaas Configuration (para testes)
asaas.api.key=test_asaas_api_key

logging.level.br.com.nhac.backend_nhac.services.TwilioSmsService=OFF
